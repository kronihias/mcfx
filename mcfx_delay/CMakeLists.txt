SET (SPECIFIC_SOURE_DIR ${SRC_DIR}/mcfx_delay)


IF(${NUM_CHANNELS} LESS 10)
	SET(PLUGIN_CODE MDe${NUM_CHANNELS})
elseif(${NUM_CHANNELS} LESS 100)
	SET(PLUGIN_CODE MD${NUM_CHANNELS})
else()
	SET(PLUGIN_CODE D${NUM_CHANNELS})
endif()


INCLUDE(${SRC_DIR}/Subproject.cmake)
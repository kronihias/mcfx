[pytest]
testpaths = .
addopts = -v -m "not soak"
# Custom flag registered by test_filter.py for golden file updates:
#   pytest tests/ --update-golden
markers =
    soak: long-running stability tests (hours). Excluded by default;
        run explicitly with: pytest -m soak
